{
  "name": "MobileReefJS",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "resolutions": {
    "@polkadot/wasm-crypto": "4.6.1"
  },
  "scripts": {
    "build:clean": "rm -rf node_modules && rm -f yarn.lock&&yarn install&&yarn start0",
    "start0": "yarn --cwd ./packages/reef-mobile-js start",
    "start1": "yarn --cwd ./packages/dApp-js start"
  }
}
