import {stealthex} from "@reef-chain/util-lib";

const {
  listCurrencies,
  getEstimatedExchange,
  createExchange,
  setTransactionHash,
  getExchangeRange
} = stealthex;

export default{
    listCurrencies,
    getEstimatedExchange,
    createExchange,
    setTransactionHash,
    getExchangeRange
}